import { createTheme } from "@mui/material";

export const WhiteFontTheme = createTheme({
  palette: {
    primary: {
      main: "#FFFFFF",
    }
  },
  typography: {
    allVariants: {
      color: "#FFFFFF",
    },
  },
});
